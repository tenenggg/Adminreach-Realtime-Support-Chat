# Backend server settings
PORT=5000
IP_ADDRESS=127.0.0.1

# PostgreSQL connection
DB_USER=postgres
DB_PASSWORD=change_me
DB_HOST=localhost
DB_PORT=5432
DB_NAME=adminreach

# JWT signing key (use a long random secret)
JWT_SECRET=replace_with_a_strong_secret
